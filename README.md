# Тестовое задание: Тестирование API заявок

## 1. Краткий тест-план

### Что проверяем

| Область | Содержание |
|---------|------------|
| **Создание заявки** | Валидация полей, обязательность, граничные значения, статус `new` по умолчанию |
| **Редактирование заявки** | Разрешённые поля, запрет на изменение `status`, поведение в разных статусах |
| **Статусная модель** | Разрешённые и запрещённые переходы для администратора |
| **Авторизация и доступ** | Изоляция заявок пользователей, права администратора |
| **Обработка ошибок** | Осмысленные сообщения, корректные HTTP-коды |
| **Граничные значения** | Предельные длины строк, границы `age` |

### Виды тестирования

- **Функциональное** — проверка всех правил создания и редактирования
- **Позитивное/негативное** — корректные и некорректные входные данные
- **Граничных значений** — `name` (1, 2, 50, 51), `age` (17, 18, 65, 66), `comment` (500, 501)
- **Тестирование статусной модели** — все разрешённые и запрещённые переходы
- **Тестирование безопасности** — изоляция данных, подмена ID, эскалация привилегий
- **API-тестирование** — через RestAssured
- **Автоматизированное регрессионное** — smoke-сьют на критический путь

### Риски

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| Race condition при смене статуса | Средняя | Высокое | Тесты на параллельные запросы |
| Утечка чужих заявок через перебор ID | Средняя | Критичное | Тесты безопасности |
| Неинформативные сообщения об ошибках | Высокая | Низкое | Проверка тела ошибки |
| Инъекции в текстовые поля | Низкая | Критичное | Негативные тесты с XSS/SQL |
| Отсутствие rate limiting | Средняя | Среднее | Нагрузочное тестирование |

---

## 2. Чек-лист

### Создание заявки (`POST /api/tickets`)

- [ ] Создание с полями `name` + `phone` (минимальный набор) → `201`
- [ ] Создание со всеми полями → `201`
- [ ] Автоматическое присвоение `status = "new"`
- [ ] Попытка передать `status` при создании → игнорируется/ошибка
- [ ] `name = "Ab"` (2 символа) → `201`
- [ ] `name = "A"` (1 символ) → `422`
- [ ] `name = 50 символов` → `201`
- [ ] `name = 51 символ` → `422`
- [ ] `name` пустое / `null` / отсутствует → `422`
- [ ] `phone` пустое / `null` / отсутствует → `422`
- [ ] `phone` невалидный формат → `422`
- [ ] `email` пустой → `201`
- [ ] `email` валидный → `201`
- [ ] `email` невалидный → `422`
- [ ] `comment = 500 символов` → `201`
- [ ] `comment = 501 символ` → `422`
- [ ] `age = 18` → `201`
- [ ] `age = 65` → `201`
- [ ] `age = 17` → `422`
- [ ] `age = 66` → `422`
- [ ] `age = "строка"` → `422`
- [ ] Создание без авторизации → `401`

### Редактирование заявки (`PATCH /api/tickets/{id}`)

- [ ] Редактирование `name` в статусе `new` → `200`
- [ ] Редактирование `phone` в статусе `new` → `200`
- [ ] Редактирование `email` в статусе `new` → `200`
- [ ] Редактирование `comment` в статусе `new` → `200`
- [ ] Редактирование `age` в статусе `new` → `200`
- [ ] Попытка изменить `status` пользователем → `422`/игнорируется
- [ ] Редактирование в статусе `in_progress` → `200`
- [ ] Редактирование в статусе `done` → `200`
- [ ] Редактирование в статусе `cancelled` → `422`
- [ ] Редактирование чужой заявки → `403`/`404`
- [ ] Частичное обновление (только одно поле) → `200`, остальные не изменились
- [ ] Валидация полей при редактировании работает как при создании

### Типы данных и формат запроса (Негативные)
- [ ] Отправка невалидного JSON (нарушена структура: пропущена запятая/кавычка) → 400 Bad Request
- [ ] Отправка пустого тела запроса (Empty Body) при POST/PATCH → 400/422
- [ ] Передача phone как integer вместо string → 400/422
- [ ] Передача age как float (дробное число, например 18.5) → 400/422
- [ ] Передача age со спецсимволами/пробелами внутри строки (если убрать валидацию строки) → 422
- [ ] Отправка дополнительных нерелевантных полей, не описанных в схеме (например, {"unknown_field": "value"}) → 201 (игнорируются) или 400 (зависит от строгой настройки десериализатора)

### Статусная модель (`PATCH /api/tickets/{id}` админом)

- [ ] `new → in_progress` → `200`
- [ ] `new → cancelled` → `200`
- [ ] `new → done` → `200` (если разрешён прямой переход)
- [ ] `in_progress → done` → `200`
- [ ] `in_progress → cancelled` → `200`
- [ ] `done → cancelled` → `200`
- [ ] `done → new` → `422`
- [ ] `done → in_progress` → `422`
- [ ] `cancelled → любой статус` → `422`
- [ ] Попытка смены статуса пользователем → `403`/`422`

### Безопасность и доступ

- [ ] Пользователь не видит чужие заявки (`GET`) → `403`/`404`
- [ ] Пользователь не редактирует чужие заявки (`PATCH`) → `403`/`404`
- [ ] Админ видит все заявки → `200`
- [ ] Неаутентифицированные запросы → `401`

---

## 3. Тест-кейсы

### TC-01: Создание заявки с минимальными полями (позитивный)

| Параметр | Значение |
|----------|----------|
| **Приоритет** | High |
| **Предусловия** | Пользователь авторизован, роль `user` |
| **Шаги** | `POST /api/tickets` → `{"name": "Иван", "phone": "+79161234567"}` |
| **Ожидаемый результат** | `201 Created`. Ответ содержит `id`, `status = "new"`, `name = "Иван"`, `phone = "+79161234567"`. `email`/`comment`/`age` = `null` |

---

### TC-02: Создание заявки со всеми полями (позитивный)

| Параметр | Значение |
|----------|----------|
| **Приоритет** | Medium |
| **Предусловия** | Пользователь авторизован |
| **Шаги** | `POST /api/tickets` → `{"name": "Анна Смирнова", "phone": "+79001234567", "email": "anna@test.ru", "comment": "Срочно", "age": 25}` |
| **Ожидаемый результат** | `201 Created`. Все переданные поля сохранены без изменений |

---

### TC-03: name = 1 символ — ниже границы (негативный, граничное)

| Параметр | Значение |
|----------|----------|
| **Приоритет** | Medium |
| **Предусловия** | Пользователь авторизован |
| **Шаги** | `POST /api/tickets` → `{"name": "A", "phone": "+79161234567"}` |
| **Ожидаемый результат** | `422 Unprocessable Entity`. `{"errors": {"name": "Имя должно содержать от 2 до 50 символов"}}` |

---

### TC-04: name = 50 символов — верхняя граница (позитивный, граничное)

| Параметр | Значение |
|----------|----------|
| **Приоритет** | Medium |
| **Предусловия** | Пользователь авторизован |
| **Шаги** | `POST /api/tickets` → `{"name": "Строка из 50 символов "A"", "phone": "+79161234567"}` |
| **Ожидаемый результат** | `201 Created`. `name` состоит ровно из 50 символов |

---

### TC-05: name = 51 символ — выше границы (негативный, граничное)

| Параметр | Значение |
|----------|----------|
| **Приоритет** | Low |
| **Предусловия** | Пользователь авторизован |
| **Шаги** | `POST /api/tickets` → `{"name": "Строка из 51 символов "A"", "phone": "+79161234567"}` |
| **Ожидаемый результат** | `422`. Сообщение о превышении максимальной длины имени |

---

### TC-06: age = 17 — ниже границы (негативный, граничное)

| Параметр | Значение |
|----------|----------|
| **Приоритет** | Medium |
| **Предусловия** | Пользователь авторизован |
| **Шаги** | `POST /api/tickets` → `{"name": "Тест", "phone": "+79161234567", "age": 17}` |
| **Ожидаемый результат** | `422`. `{"errors": {"age": "Возраст должен быть от 18 до 65"}}` |

---

### TC-07: age = 66 — выше границы (негативный, граничное)

| Параметр | Значение |
|----------|----------|
| **Приоритет** | Medium |
| **Предусловия** | Пользователь авторизован |
| **Шаги** | `POST /api/tickets` → `{"name": "Тест", "phone": "+79161234567", "age": 66}` |
| **Ожидаемый результат** | `422`. Сообщение о недопустимом возрасте |

---

### TC-08: email невалидный (негативный)

| Параметр | Значение |
|----------|----------|
| **Приоритет** | Medium |
| **Предусловия** | Пользователь авторизован |
| **Шаги** | `POST /api/tickets` → `{"name": "Тест", "phone": "+79161234567", "email": "not-an-email"}` |
| **Ожидаемый результат** | `422`. `{"errors": {"email": "Некорректный формат email"}}` |

---

### TC-09: Редактирование в статусе cancelled (негативный)

| Параметр | Значение |
|----------|----------|
| **Приоритет** | High |
| **Предусловия** | Заявка пользователя находится в статусе `cancelled` |
| **Шаги** | `PATCH /api/tickets/{id}` → `{"name": "Новое имя"}` |
| **Ожидаемый результат** | `422`. `{"error": "Нельзя редактировать отменённую заявку"}` |

---

### TC-10: Доступ к чужой заявке (негативный, безопасность)

| Параметр | Значение |
|----------|----------|
| **Приоритет** | Critical |
| **Предусловия** | Пользователь A авторизован. Существует заявка `id=100` пользователя B |
| **Шаги** | `GET /api/tickets/100` с токеном пользователя A |
| **Ожидаемый результат** | `404 Not Found` (чтобы не раскрывать факт существования) |

---

### TC-11: Админ: done → new запрещён (негативный, статусная модель)

| Параметр | Значение |
|----------|----------|
| **Приоритет** | Critical |
| **Предусловия** | Админ авторизован. Заявка `id=1` в статусе `done` |
| **Шаги** | `PATCH /api/tickets/1` → `{"status": "new"}` |
| **Ожидаемый результат** | `422`. `{"error": "Нельзя перевести заявку из done в new"}` |

---

### TC-12: Админ: new → in_progress (позитивный, статусная модель)

| Параметр | Значение |
|----------|----------|
| **Приоритет** | High |
| **Предусловия** | Админ авторизован. Заявка `id=1` в статусе `new` |
| **Шаги** | `PATCH /api/tickets/1` → `{"status": "in_progress"}` |
| **Ожидаемый результат** | `200 OK`. `status = "in_progress"`, остальные поля без изменений |

---

### TC-13 (дополнительный): Попытка передать status при создании

| Параметр | Значение |
|----------|----------|
| **Приоритет** | High |
| **Предусловия** | Пользователь авторизован |
| **Шаги** | `POST /api/tickets` → `{"name": "Тест", "phone": "+79161234567", "status": "done"}` |
| **Ожидаемый результат** | `201`, но `status` принудительно = `"new"` (поле игнорируется) |

---

## 4. Баг-репорты

### Баг-репорт #1

| Поле | Описание |
|------|----------|
| **ID** | BUG-001 |
| **Заголовок** | Администратор может перевести заявку из `done` обратно в `new` |
| **Приоритет** | Critical |
| **Серьёзность** | Blocker |
| **Окружение** | Staging, build 2.4.1, 19.06.2026 |
| **Шаги воспроизведения** | 1. Авторизоваться как `admin`<br>2. Создать заявку через `POST /api/tickets`<br>3. Перевести заявку в `done`: `PATCH /api/tickets/42 {"status": "done"}`<br>4. Попытаться вернуть в `new`: `PATCH /api/tickets/42 {"status": "new"}` |
| **Фактический результат** | Статус меняется на `new`, код `200` |
| **Ожидаемый результат** | `422 Unprocessable Entity`, тело: `{"error": "Нельзя перевести заявку из done в new"}` |
| **Вложения** | `curl -X PATCH https://api.staging.example.com/api/tickets/42 -H "Authorization: Bearer admin_xxx" -H "Content-Type: application/json" -d '{"status": "new"}'` → `200 OK` |

---

### Баг-репорт #2

| Поле | Описание |
|------|----------|
| **ID** | BUG-002 |
| **Заголовок** | Пользователь может редактировать заявку в статусе `cancelled` |
| **Приоритет** | High |
| **Серьёзность** | Major |
| **Окружение** | Staging, build 2.4.1, 19.06.2026 |
| **Шаги воспроизведения** | 1. Авторизоваться как `user` (владелец заявки)<br>2. Дождаться/добиться перевода заявки в `cancelled`<br>3. Выполнить `PATCH /api/tickets/42 {"name": "Взломанное имя"}` |
| **Фактический результат** | `200 OK`, имя изменено |
| **Ожидаемый результат** | `422`, `{"error": "Нельзя редактировать отменённую заявку"}` |
| **Вложения** | Ответ сервера: `{"data": {"id": 42, "name": "Взломанное имя", "status": "cancelled", ...}}` |

---

## 5. API-проверки

### Запрос 1: Успешное создание заявки

POST /api/tickets
Authorization: Bearer <user_token>
Content-Type: application/json

{
"name": "Иван Петров",
"phone": "+79161234567",
"email": "ivan@example.com",
"comment": "Тестовая заявка",
"age": 30
}


| Параметр | Значение |
|----------|----------|
| **Ожидаемый статус** | `201 Created` |
| **Что проверяем** | `data.status == "new"`, `data.name == "Иван Петров"`, наличие `data.id` (число > 0), `data.createdAt` (ISO 8601) |

---

### Запрос 2: Валидация name (1 символ)

POST /api/tickets
Authorization: Bearer <user_token>
Content-Type: application/json

{
"name": "A",
"phone": "+79161234567"
}


| Параметр | Значение |
|----------|----------|
| **Ожидаемый статус** | `422 Unprocessable Entity` |
| **Что проверяем** | `errors.name` содержит текст "от 2 до 50" или "минимум 2", ответ — JSON, нет поля `data` |

---

### Запрос 3: Попытка смены статуса пользователем

PATCH /api/tickets/1
Authorization: Bearer <user_token>
Content-Type: application/json

{
"status": "done"
}


| Параметр | Значение |
|----------|----------|
| **Ожидаемый статус** | `403 Forbidden` или `422` |
| **Что проверяем** | Сообщение об ошибке содержит "доступ" / "запрещено" / "нельзя менять статус" |

---

### Запрос 4: Админ: done → new (должен быть запрет)

PATCH /api/tickets/1
Authorization: Bearer <admin_token>
Content-Type: application/json

{
"status": "new"
}


| Параметр | Значение |
|----------|----------|
| **Ожидаемый статус** | `422 Unprocessable Entity` |
| **Что проверяем** | `error` / `message` содержит "нельзя" и "done" и "new" |

---

### Запрос 5: Доступ к чужой заявке

GET /api/tickets/999
Authorization: Bearer <user_token>


| Параметр | Значение |
|----------|----------|
| **Ожидаемый статус** | `404 Not Found` (предпочтительно) |
| **Что проверяем** | Ответ не содержит данных заявки, есть сообщение "не найдена" |

---

### Запрос 6: Редактирование в cancelled

PATCH /api/tickets/42
Authorization: Bearer <user_token_owner>
Content-Type: application/json

{
"comment": "Попытка изменить отменённую заявку"
}


| Параметр | Значение |
|----------|----------|
| **Ожидаемый статус** | `422 Unprocessable Entity` |
| **Что проверяем** | `error` содержит "отменён" / "cancelled" и "нельзя редактировать" |

---

### Запрос 7: comment = 500 символов (граница)

POST /api/tickets
Authorization: Bearer <user_token>
Content-Type: application/json

{
"name": "Граничный тест",
"phone": "+79999999999",
"comment": "AAAAAAAAAA... (500 символов)"
}


| Параметр | Значение |
|----------|----------|
| **Ожидаемый статус** | `201 Created` |
| **Что проверяем** | `data.comment.length == 500`, комментарий сохранён полностью, без обрезки |
